@using System.ComponentModel.DataAnnotations.Schema
@using Microsoft.AspNetCore.Mvc.ModelBinding
@using Trivago.Core.Ubicacion;
@model HomeViewModel
@using Trivago.MVC;

@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
<form asp-action="Index" method="post">
    <input hidden asp-for="@Model.homePostViewModel.LugaresDato" value="@Model.homePostViewModel.LugaresDato">
    <select asp-for="@Model.homePostViewModel.id"  asp-items="@Model.homePostViewModel.Lugares">
        <option disabled selected>--Elegir un Lugar--</option>
    </select>
    <button type="submit">Buscar
    </button>
</form>
    
    <a asp-controller="Ciudad" asp-action="Index">Ver ciudades</a>
    <a asp-controller="Pais" asp-action="Index">Ver paises</a>

</div>
